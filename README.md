# 🚀 @arifbiswas/app-expo-cli

**App Expo CLI** হলো একটি শক্তিশালী কমান্ড-লাইন টুল যা আপনার এক্সপো (Expo) প্রজেক্টের জন্য একটি স্ট্যান্ডার্ড এবং প্রফেশনাল ফোল্ডার স্ট্রাকচার (Architecture) অটোমেটিক সেটআপ করে দেয়। এটি ব্যবহার করলে আপনাকে বারবার একই ফোল্ডার বা Redux সেটাআপ ম্যানুয়ালি করতে হবে না।

---

## ✨ Features

- **Standard Architecture:** প্রজেক্ট তৈরি হওয়ার পর অটোমেটিক `src/` ফোল্ডারের ভেতরে `hooks`, `redux`, `utils`, `components` ইত্যাদি সাজিয়ে দেয়।
- **Interactive Setup:** Clack প্রম্পট ব্যবহার করে ইউজারকে জিজ্ঞেস করে সে **Tabs** বা **Drawer** নেভিগেশন চায় কি না।
- **External Lib Integration:** সরাসরি গিটহাব থেকে লেটেস্ট `lib` ফোল্ডার ক্লোন করার সুবিধা।
- **Redux Ready:** `@reduxjs/toolkit` এবং `react-redux` অটোমেটিক ইন্সটল এবং কনফিগার করে দেয়।
- **Pre-built Screens:** `auth`, `settings`, `modals`, এবং `common` স্ক্রিনগুলোর টেমপ্লেট অটোমেটিক সেটআপ হয়ে যায়।
- **Tailwind Ready:** `tailwind.config.js` ফাইলটি অটোমেটিক রুট ডিরেক্টরিতে কপি করে দেয়।

---

## 📦 Installation

আপনি এটি ইন্সটল না করেই সরাসরি `npx` দিয়ে ব্যবহার করতে পারেন (সবচেয়ে ভালো উপায়):

```bash
npx @arifbiswas/app-expo-cli my-awesome-app
```
